---
import BlurFade from '../components/Grace/magic-ui/BlurFadeIn';
import HomeLayout from './HomeLayout.astro';
import dayjs from 'dayjs';
const { frontmatter } = Astro.props;
console.log(frontmatter);
---

<HomeLayout>
	<div class="flex flex-col gap-8 max-w-5xl text-white">
		<div class="text-3xl font-bold leading-tight text-white mb-2">
			<h1>{frontmatter.title}</h1>
		</div>
		<div class="flex gap-4 items-center">
			<div class="font-bold text-gray-300 text-lg">
				{dayjs(frontmatter.date).format('YYYY-MM-DD')}
			</div>
		</div>

		<!-- <div class="flex flex-col items-start text-white gap-2">
			<div class="text-center text-3xl font-bold leading-tight text-white m-auto">
				<h1>{frontmatter.title}</h1>
			</div>
			<div class="flex gap-4 items-center">
				<div>{dayjs(frontmatter.date).format('YYYY-MM-DD')}</div>
			</div>
		</div> -->
		<div
			class="prose dark:prose-invert
	  prose-h1:font-bold max-w-full prose-h1:text-xl
	  prose-a:text-blue-600 prose-p:text-justify prose-img:rounded-xl"
		>
			<slot />
		</div>
	</div>
</HomeLayout>
