---
const { title } = Astro.props;
import { cn } from '@/lib/utils';
const { className = {} } = Astro.props;
---

<div
	class={cn(
		'text-3xl text-white mx-auto mt-40 mb-10 font-serif font-bold text-center tracking-wide',
		className
	)}
>
	{title}
</div>
